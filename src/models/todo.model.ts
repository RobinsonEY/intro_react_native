export interface ITodo {
    id: string
    done: boolean
    text: string
    color: string
  }

  export interface ITodoPlaceHolder{
    userId: number,
    id: number,
    title: string,
    completed: boolean
  }
